# Deprecated Telemetry Specification Link

> **Audience: AI agents following a legacy link.** Do not load this page in addition to its owner.

The current Phase A status, subsystem boundary, transport invariants, and video separation contract moved to [`../reference/telemetry-contract.md`](../reference/telemetry-contract.md).

Invocation details remain in [`../../runs/telemetry/README.md`](../../runs/telemetry/README.md). This compatibility page preserves that runtime README's existing link.

## AI Checklist

- Have I loaded `reference/telemetry-contract.md` as the only telemetry design context?
- Have I avoided assuming production mission-state integration exists?
